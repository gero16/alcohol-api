DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/alcoholes?schema=public"
FULL_BACKUP_CACHE_TTL_MS=300000
# Opcional: ruta del JSON estático de guías (por defecto static/guides.json bajo cwd del API; servido en /static/guides.json)
# GUIDES_STATIC_JSON_PATH=
